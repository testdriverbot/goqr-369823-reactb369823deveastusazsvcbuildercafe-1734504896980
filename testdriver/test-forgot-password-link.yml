version: 4.1.30
steps:
  - prompt: Locate the "Forgot password?" link on the login page.
    commands:
      - command: hover-text
        text: Forgot password?
        description: link for password recovery on the login page
        action: click
  - prompt: Click the "Forgot password?" link.
    commands: []
  - prompt: >-
      Assert that the password recovery page loads successfully.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the "Forgot password" text is visible on the page
