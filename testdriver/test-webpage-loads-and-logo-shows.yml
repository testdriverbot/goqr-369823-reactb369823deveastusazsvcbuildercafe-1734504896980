version: 4.1.30
steps:
  - prompt: Open the browser.
    commands: []
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: the webpage displays the Login form
  - prompt: >-
      Assert that the logo "GoQR" is displayed on the page.  # Use the content
      of the file from the environment
    commands:
      - command: assert
        expect: the GoQR logo is displayed on the page
